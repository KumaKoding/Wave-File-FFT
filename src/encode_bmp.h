#ifndef __ENCODE_BMP_H__
#define __ENCODE_BMP_H__

#include <stdio.h>
#include <stdlib.h>
#include <time.h>

void encode(uint8_t *image, uint32_t width, uint32_t height, const char * const name);

#endif